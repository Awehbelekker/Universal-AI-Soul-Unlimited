# Optional desktop voice dependencies
edge-tts>=6.1.0          # neural Microsoft voices (preferred default)
pyttsx3>=2.90            # robotic system TTS fallback
SpeechRecognition>=3.10.0
PyAudio>=0.2.14
faster-whisper>=1.0.0    # offline STT (preferred); falls back to Google if missing
# CUDA 12 runtime libs for faster-whisper GPU on Windows (CTranslate2 needs cublas64_12):
nvidia-cublas-cu12
nvidia-cudnn-cu12
nvidia-cuda-nvrtc-cu12
nvidia-cufft-cu12
nvidia-cuda-runtime-cu12
# Or install full toolkit: https://developer.nvidia.com/cuda-12-6-0-download-archive
# (CUDA Toolkit 12.x — not 13.x; driver can be newer)

# Voice activity detection (local-first):
#   Energy-based VAD works with numpy alone (already a core dep).
#   For higher-accuracy Silero VAD, install torch — model auto-downloads via
#   torch.hub on first use. If torch is absent, VAD falls back to energy-based.
# torch  # optional, enables Silero VAD (also used by faster-whisper GPU / XTTS)

# Local audio cleanup (noise suppression + output enhancement):
#   Works with numpy alone; scipy (already a core dep) enables higher-quality
#   band-pass EQ / filtering. No cloud dependency.

# Premium cloud voice tier (OPT-IN — local stays the default):
#   ElevenLabs TTS / Deepgram STT activate only when a key is supplied and
#   privacy_mode is disabled. Uses aiohttp (already a core dep). Set keys via:
#     ELEVENLABS_API_KEY=...   DEEPGRAM_API_KEY=...
#   or VoiceConfig(elevenlabs_api_key=..., deepgram_api_key=...).

# Natural offline neural TTS — Kokoro-82M (preferred natural/offline default):
#   High-quality (MOS ~4.4), low-latency (~0.3s first audio on a modest GPU),
#   fully offline once the 82M model + voices download on first use. Runs on
#   CUDA when available, else CPU. 54 built-in Apache-2.0 voices (no cloning).
#   Override the default voice with SOUL_KOKORO_VOICE (e.g. af_heart).
kokoro>=0.9.4            # Kokoro-82M pipeline (pulls misaki + bundled espeak-ng)
soundfile>=0.12.1        # write Kokoro float32 audio to WAV bytes

# Offline neural TTS (zero-network default when edge-tts is unavailable):
#   Uses the same coqui-tts package below with a local single-speaker model
#   (tts_models/en/ljspeech/tacotron2-DDC by default). The model downloads once
#   on first use, then synthesis runs fully offline — no network, no reference
#   WAV. Falls back to robotic pyttsx3 only if coqui-tts is absent. Override the
#   model with SOUL_LOCAL_TTS_MODEL. Installing coqui-tts (below) enables both
#   this offline neural voice and voice cloning.

# Voice cloning + offline neural TTS (heavy — use setup script):
#   python scripts/setup_voice_clone.py
# Then: voice clone path/to/sample.wav
# Use coqui-tts (not the deprecated TTS package — that needs MSVC on Windows).
# coqui-tts>=0.27.0
# torch (CUDA 11.8 recommended for GTX 10-series)
